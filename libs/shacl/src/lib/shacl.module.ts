import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

// TODO delete me, will come in next PR
@NgModule({
  imports: [CommonModule],
})
export class ShaclModule {}
