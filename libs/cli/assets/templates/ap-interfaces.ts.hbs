// this file was generated using cz-cli, it is advised to not update it manually and use the cli instead

import { JsonModel, JsonModelType } from '@cognizone/json-model';
import { LangString } from '@cognizone/model-utils';

{{#each imports}}
import { {{element}} } from '{{from}}';
{{/each}}

{{#each types}}
export interface {{this.name}} extends {{extendClass}} {
  '@type': JsonModelType<[{{{classIds}}}]>;
  {{#if facets}}
  '@facets'?: {{facets}};
  {{/if}}
  {{#each attributes}}
  {{this.name}}{{#if isOptional}}?{{/if}}: {{this.type}};
  {{/each}}
}

{{/each}}
